version: '3.8'

services:
  depocom-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: depocom_server_uho
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
    restart: always